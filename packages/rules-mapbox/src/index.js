/* @nac-spec/rules-mapbox -- skeleton, phase 4 fills out. */
'use strict';
module.exports = function applyMapboxRules(NAC) {
  if (!NAC || !NAC.adopt) throw new Error('NAC v2.0 with adopt() required');
  /* Mapbox GL JS canvas + control selectors. Phase 4 covers:
     .mapboxgl-ctrl-zoom-in, .mapboxgl-ctrl-zoom-out, markers,
     popups, drawing tools. */
};
