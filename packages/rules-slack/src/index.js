/* @nac-spec/rules-slack -- skeleton, phase 4 fills out. */
'use strict';
module.exports = function applySlackRules(NAC) {
  if (!NAC || !NAC.adopt) throw new Error('NAC v2.0 with adopt() required');
  /* Slack widget selectors (subject to change with Slack updates):
     [data-qa="message"], [data-qa="reaction-list"], [data-qa="threads"]... */
  /* Phase 4: complete rule set with versioned selectors. */
};
