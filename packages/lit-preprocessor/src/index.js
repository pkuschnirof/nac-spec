/* @nac-spec/lit-preprocessor -- skeleton (v2.0-rc2 Mistral T7-F1).
   Lit html`` template tag auto-anotation + automatic
   bridgeShadowRoot registration on first connectedCallback.
   Phase 4 fills out via TS transformer + decorator hook. */
'use strict';
module.exports = function nacLitPreprocessor() {
  return {
    name: '@nac-spec/lit-preprocessor',
    transform: function (code, id) { return null; }
  };
};
